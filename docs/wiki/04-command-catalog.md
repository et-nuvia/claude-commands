# Command Catalog

> **Stub.** Auto-generated catalog of all `commands/*.md` files grouped by
> workflow phase (task lifecycle, deploy, infra, db, rca, code quality,
> etc.) will be added during content migration. A `scripts/build-catalog.sh`
> helper will regenerate this page from command frontmatter so it stays in
> sync.
