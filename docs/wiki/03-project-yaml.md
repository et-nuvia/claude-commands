# PROJECT.yaml

Every project that uses these commands has a `PROJECT.yaml` at its root.
Scripts auto-read it to learn the project's tech stack, deployment targets,
task management backend, and secrets configuration.

> **Stub.** Full reference will be ported from `~/.claude/docs/project-config.md`
> during content migration. For now, see `templates/PROJECT.yaml.example` in
> this repo.
